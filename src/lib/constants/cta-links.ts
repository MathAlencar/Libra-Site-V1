/**
 * Links de conversão — substituir pelos URLs definitivos quando disponíveis.
 */
export const CTA_LINKS = {
  SIMULAR_AGORA: '#',
  WHATSAPP: '#',
  CONTATO: '#',
  PARCEIROS_FORM: '#',
} as const

export type CtaLinkKey = keyof typeof CTA_LINKS

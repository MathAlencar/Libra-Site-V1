
    --navy:#0b1f3f;
    --navy-2:#12294d;
    --blue:#34548c;
    --blue-deep:#284368;
    --gold:#e6a83c;
    --gold-deep:#7a5410;
    --cream:#f6f1e6;
    --cream-2:#efe7d5;
    --ink:#0b1f3f;
    --muted:#5a6b84;
    --muted-light:rgba(246,241,230,.72);
    --line:rgba(11,31,63,.12);
    --line-light:rgba(246,241,230,.16);
    --radius:14px;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  html{scroll-behavior:smooth}
  body{font-family:"Instrument Sans",system-ui,sans-serif;background:var(--cream);color:var(--ink);line-height:1.65;-webkit-font-smoothing:antialiased;overflow-x:hidden}
  .wrap{max-width:1200px;margin:0 auto;padding:0 30px}
  .serif{font-family:"Instrument Serif",Georgia,serif;font-weight:500;line-height:1.03;letter-spacing:-.015em}
  .em{font-style:italic;color:var(--gold);font-weight:500}
  a{color:inherit;text-decoration:none}
  img{display:block}
  :focus-visible{outline:2.5px solid var(--gold);outline-offset:3px;border-radius:4px}

  .eyebrow{font-size:12.5px;font-weight:700;letter-spacing:.15em;text-transform:uppercase;color:var(--gold-deep);display:inline-flex;align-items:center;gap:10px}
  .eyebrow::before{content:"";width:24px;height:2px;background:var(--gold)}
  .eyebrow.light{color:var(--gold)}

  /* buttons */
  .btn{display:inline-flex;align-items:center;gap:9px;font-family:"Instrument Sans",sans-serif;font-size:15.5px;font-weight:600;padding:15px 27px;border-radius:100px;cursor:pointer;border:1.6px solid transparent;transition:transform .18s ease,background .18s ease,box-shadow .18s ease}
  .btn .ar{transition:transform .2s ease}
  .btn:hover .ar{transform:translateX(4px)}
  .btn-blue{background:var(--blue);color:#fff}
  .btn-blue:hover{transform:translateY(-2px);box-shadow:0 14px 30px -12px rgba(52,84,140,.7)}
  .btn-gold{background:var(--gold);color:var(--navy)}
  .btn-gold:hover{transform:translateY(-2px);box-shadow:0 14px 30px -12px rgba(230,168,60,.75)}
  .btn-ghost-l{background:transparent;border-color:var(--line-light);color:var(--cream)}
  .btn-ghost-l:hover{border-color:var(--cream);background:rgba(246,241,230,.08)}
  .btn-ghost-d{background:transparent;border-color:var(--line);color:var(--ink)}
  .btn-ghost-d:hover{border-color:var(--navy);background:rgba(11,31,63,.05)}
  .btn-sm{padding:11px 21px;font-size:14.5px}

  /* header */
  header{position:fixed;top:0;left:0;right:0;z-index:60;transition:background .3s ease,box-shadow .3s ease}
  header .wrap{display:flex;align-items:center;justify-content:space-between;height:78px}
  header .navlinks a,header .login{color:var(--cream)}
  header.scrolled{background:rgba(246,241,230,.94);backdrop-filter:blur(12px);box-shadow:0 1px 0 var(--line)}
  header.scrolled .navlinks a,header.scrolled .login,header.scrolled .brand{color:var(--ink)}
  .brand{display:flex;align-items:center;gap:11px;font-family:"Instrument Serif",serif;font-size:27px;font-weight:500;letter-spacing:-.02em;color:var(--cream)}
  .navlinks{display:flex;gap:32px;font-size:15px;font-weight:500}
  .navlinks a{opacity:.85;transition:opacity .15s}
  .navlinks a:hover{opacity:1}
  .navr{display:flex;align-items:center;gap:22px}
  .login{font-size:14.5px;font-weight:600}

  /* photo holder w/ fallback */
  .ph{position:relative;overflow:hidden;background:linear-gradient(150deg,var(--navy-2),var(--navy));border-radius:20px}
  .ph img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}
  .ph .fb{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;color:var(--muted-light);text-align:center;padding:24px;font-size:13.5px;font-weight:500}
  .ph .fb svg{opacity:.5}

  /* hero */
  .hero{background:var(--navy);color:var(--cream);padding:150px 0 90px;position:relative;overflow:hidden}
  .hero::after{content:"";position:absolute;top:-160px;right:-120px;width:520px;height:520px;border-radius:50%;background:var(--blue);opacity:.16;filter:blur(30px)}
  .hero-grid{display:grid;grid-template-columns:1.08fr .92fr;gap:60px;align-items:center;position:relative;z-index:2}
  h1{font-size:clamp(46px,6.6vw,90px)}
  .hero .lead{font-size:19.5px;color:var(--muted-light);max-width:30em;margin:26px 0 34px;line-height:1.6}
  .hero-cta{display:flex;flex-wrap:wrap;gap:14px}
  .microtrust{display:flex;flex-wrap:wrap;gap:9px 20px;margin-top:28px;font-size:14px;color:var(--muted-light);font-weight:500}
  .microtrust span{display:inline-flex;align-items:center;gap:8px}
  .dot{width:6px;height:6px;border-radius:50%;background:var(--gold)}
  .hero-photo{position:relative;aspect-ratio:1/1.16}
  .hero-photo .ph{width:100%;height:100%;border-radius:24px}
  .chip{position:absolute;background:rgba(246,241,230,.94);backdrop-filter:blur(6px);border-radius:14px;padding:13px 17px;box-shadow:0 18px 40px -18px rgba(0,0,0,.5);z-index:3}
  .chip small{display:block;font-size:11.5px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;color:var(--gold-deep)}
  .chip b{font-family:"Instrument Serif",serif;font-size:24px;font-weight:600;color:var(--navy);line-height:1.05}
  .chip.c1{left:-26px;bottom:64px}
  .chip.c2{right:-18px;top:52px;background:var(--gold);color:var(--navy)}
  .chip.c2 small{color:var(--gold-deep)}

  /* ticker strip */

  /* sections */
  .sec{padding:104px 0}
  .sec--cream{background:var(--cream)}
  .sec--cream2{background:var(--cream-2)}
  .sec--navy{background:var(--navy);color:var(--cream)}
  .sec--blue{background:var(--blue);color:#fff}
  .sec-head{max-width:40rem;margin-bottom:56px}
  .sec-head h2{font-size:clamp(33px,4.4vw,54px);margin-top:16px}
  .sec-head p{color:var(--muted);font-size:18px;margin-top:16px}
  .sec--navy .sec-head p{color:var(--muted-light)}

  /* pra quê: editorial split */
  .split{display:grid;grid-template-columns:.95fr 1.05fr;gap:56px;align-items:center}
  .split .ph{aspect-ratio:4/5;border-radius:22px}
  .uselist{display:flex;flex-direction:column;gap:6px}
  .use{display:flex;gap:20px;padding:22px 4px;border-bottom:1px solid var(--line);align-items:flex-start}
  .use:last-child{border-bottom:none}
  .use .ui{flex:none;width:46px;height:46px;border-radius:12px;background:var(--cream-2);display:flex;align-items:center;justify-content:center;color:var(--blue);font-size:22px;font-weight:700}
  .use h3{font-family:"Instrument Serif",serif;font-size:22px;font-weight:500;margin-bottom:5px}
  .use p{font-size:15.5px;color:var(--muted);line-height:1.55}

  /* how it works (navy) */
  .steps{display:grid;grid-template-columns:repeat(4,1fr);gap:28px}
  .step .num{font-family:"Instrument Serif",serif;font-size:52px;font-weight:500;color:var(--gold);line-height:1}
  .step .bar{height:2px;background:var(--line-light);margin:16px 0 20px;position:relative;overflow:hidden}
  .step .bar::after{content:"";position:absolute;inset:0;width:40%;background:var(--gold)}
  .step h3{font-family:"Instrument Serif",serif;font-size:22px;font-weight:500;margin-bottom:10px;color:var(--cream)}
  .step p{font-size:15px;color:var(--muted-light)}

  /* comparison (blue block) */
  .cmp-head{max-width:36rem;margin-bottom:48px}
  .cmp-head h2{font-size:clamp(33px,4.4vw,54px);margin-top:16px;color:#fff}
  .cmp-head .eyebrow{color:#fff}
  .cmp-head .eyebrow::before{background:var(--gold)}
  .cmp-grid{display:grid;grid-template-columns:1fr 1fr;gap:22px;align-items:stretch}
  .cmp{border-radius:22px;padding:40px 38px}
  .cmp-bad{background:rgba(11,31,63,.28);border:1px solid rgba(246,241,230,.18);color:#fff}
  .cmp-good{background:var(--cream);color:var(--navy)}
  .cmp small{font-size:13px;font-weight:700;letter-spacing:.09em;text-transform:uppercase;opacity:.85}
  .cmp .big{font-family:"Instrument Serif",serif;font-size:clamp(52px,8vw,84px);font-weight:500;line-height:.95;margin:14px 0 6px}
  .cmp-bad .big{color:#fff}
  .cmp-good .big{color:var(--blue)}
  .cmp .u{font-size:16px;opacity:.9}
  .cmp-meta{display:flex;gap:36px;margin-top:34px;padding-top:26px;border-top:1px solid var(--line)}
  .cmp-meta .mk{font-family:"Instrument Serif",serif;font-size:32px;color:var(--navy)}
  .cmp-meta .ml{font-size:13px;color:var(--muted)}
  .cmp-note{text-align:center;max-width:36rem;margin:34px auto 0;color:rgba(255,255,255,.9);font-size:16px}

  /* manifesto */
  .manifesto{text-align:center}
  .manifesto h2{font-family:"Instrument Serif",serif;font-size:clamp(34px,5.4vw,64px);font-weight:500;line-height:1.08;max-width:15em;margin:26px auto 22px}
  .manifesto p{color:var(--muted-light);max-width:34rem;margin:0 auto;font-size:18px}
  .manifesto .eyebrow{color:var(--gold);justify-content:center}
    .bikepath{position:relative;width:min(580px,86vw);height:70px;margin:52px auto 0}
  .bikepath-line{position:absolute;left:0;right:0;bottom:12px;border-top:3px dotted rgba(246,241,230,.38)}
  .bike-rider{position:absolute;left:0;bottom:8px;width:84px;color:var(--gold);animation:ride 10s ease-in-out infinite;will-change:transform}
  .bike-rider svg{display:block;width:100%;height:auto}
  @keyframes ride{0%{transform:translateX(0) scaleX(1)}46%{transform:translateX(calc(min(580px,86vw) - 84px)) scaleX(1)}50%{transform:translateX(calc(min(580px,86vw) - 84px)) scaleX(-1)}96%{transform:translateX(0) scaleX(-1)}100%{transform:translateX(0) scaleX(1)}}

  /* testimonial */
  .tgrid{display:grid;grid-template-columns:.8fr 1.2fr;gap:56px;align-items:center}
  .tgrid .ph{aspect-ratio:1/1.1;border-radius:22px}
  .quote .mark{font-family:"Instrument Serif",serif;font-size:84px;color:var(--gold);line-height:.5;height:36px;display:block}
  .quote blockquote{font-family:"Instrument Serif",serif;font-size:clamp(25px,3.4vw,40px);font-weight:400;line-height:1.28;letter-spacing:-.01em}
  .quote .who{margin-top:32px}
  .quote .who b{font-weight:700;font-size:16px}
  .quote .who span{display:block;font-size:14.5px;color:var(--muted)}

  /* about (navy) */
  .about-grid{display:grid;grid-template-columns:1.05fr .95fr;gap:56px;align-items:center}
  .about h2{font-family:"Instrument Serif",serif;font-size:clamp(30px,4.1vw,48px);font-weight:500;line-height:1.1;margin-top:16px}
  .about p{color:var(--muted-light);font-size:18px;margin-top:20px;max-width:34em}
  .about .eyebrow{color:var(--gold)}
  .about-photo{position:relative}
  .about-photo .ph{aspect-ratio:5/4;border-radius:22px}
  .about-stats{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:26px}
  .about-stats .s{background:rgba(246,241,230,.06);border:1px solid var(--line-light);border-radius:16px;padding:22px}
  .about-stats .s b{font-family:"Instrument Serif",serif;font-size:38px;font-weight:500;color:var(--gold);display:block;line-height:1}
  .about-stats .s span{font-size:14px;color:var(--muted-light);margin-top:8px;display:block}

  /* final */
  .final{text-align:center}
  .final h2{font-size:clamp(40px,5.8vw,72px);max-width:13em;margin:18px auto 20px;color:var(--cream)}
  .final p{color:var(--muted-light);font-size:19px;max-width:32rem;margin:0 auto 34px}

  /* footer */
  footer{background:#081733;color:var(--muted-light);padding:72px 0 40px;font-size:14.5px}
  .foot-top{display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr;gap:36px;padding-bottom:44px;border-bottom:1px solid var(--line-light)}
  .foot-top .brand{color:var(--cream);margin-bottom:14px}
  .foot-top h4{color:var(--cream);font-size:13px;letter-spacing:.08em;text-transform:uppercase;margin-bottom:16px;font-weight:700}
  .foot-top a{display:block;margin-bottom:11px;transition:color .15s}
  .foot-top a:hover{color:var(--gold)}
  .foot-tag{max-width:22em;line-height:1.6}
  .legal{margin-top:34px;font-size:12.5px;line-height:1.7;color:rgba(246,241,230,.5);display:flex;justify-content:space-between;gap:24px;flex-wrap:wrap}
  .legal p{max-width:52em}

  .reveal{opacity:0;transform:translateY(28px);transition:opacity .7s ease,transform .7s ease}
  .reveal.in{opacity:1;transform:none}

  @media (max-width:920px){
    .hero-grid,.split,.cmp-grid,.tgrid,.about-grid{grid-template-columns:1fr;gap:40px}
    .steps{grid-template-columns:1fr 1fr;gap:24px}
    .foot-top{grid-template-columns:1fr 1fr}
    .navlinks,.login{display:none}
    .hero{padding:120px 0 70px}
    .sec{padding:74px 0}
    .hero-photo{aspect-ratio:1/.82;max-width:520px}
    .tgrid .ph,.split .ph{max-width:460px}
  }
  @media (max-width:560px){
    .steps{grid-template-columns:1fr}
    .wrap{padding:0 20px}
    .chip.c1{left:6px}.chip.c2{right:6px}
  }
  @media (prefers-reduced-motion:reduce){
    *{animation:none!important;transition:none!important}
    .reveal{opacity:1;transform:none}
    html{scroll-behavior:auto}
  }

  .logo{display:inline-block;height:30px;aspect-ratio:130/59;background-color:#003399;-webkit-mask:var(--logo) center/contain no-repeat;mask:var(--logo) center/contain no-repeat}
  header .brand .logo,.hdr .brand .logo{background-color:#f6f1e6}
  header.scrolled .brand .logo,.hdr.scrolled .brand .logo{background-color:#003399}
  .foot-top .brand .logo,.foot .brand .logo,.menu .brand .logo{background-color:#f6f1e6}
  .foot-top .brand .logo,.foot .brand .logo{height:27px}
  .carousel{overflow:hidden}
  .track{display:flex;transition:transform .55s cubic-bezier(.4,0,.1,1)}
  .slide{flex:0 0 100%;width:100%;min-width:0;box-sizing:border-box}
  .track>.slide>*{min-width:0}
  .cnav{display:flex;align-items:center;justify-content:center;gap:20px;margin-top:42px}
  .cbtn{width:48px;height:48px;border-radius:50%;border:1.5px solid var(--line);background:#fff;color:var(--navy);font-size:26px;line-height:1;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:background .18s,color .18s,border-color .18s,transform .18s;font-family:"Instrument Sans",sans-serif;padding:0}
  .cbtn:hover{background:var(--navy);color:#fff;border-color:var(--navy);transform:translateY(-2px)}
  .cdots{display:flex;gap:10px;align-items:center}
  .cdot{width:10px;height:10px;border-radius:50%;background:rgba(11,31,63,.22);border:none;padding:0;cursor:pointer;transition:width .25s ease,background .25s ease}
  .cdot.active{background:var(--gold);width:28px;border-radius:5px}
  /* --- multipágina --- */
  .navlinks a.active{opacity:1;color:var(--cream);position:relative}
  header.scrolled .navlinks a.active{color:var(--navy)}
  .navlinks a.active::after{content:"";position:absolute;left:0;right:0;bottom:-6px;height:2px;background:var(--gold)}
  .foot-top a.active{opacity:1;color:var(--gold)}
  .phero{background:var(--navy);color:var(--cream);padding:150px 0 72px;position:relative;overflow:hidden}
  .phero::after{content:"";position:absolute;top:-120px;right:-100px;width:440px;height:440px;border-radius:50%;background:var(--blue);opacity:.14;filter:blur(32px)}
  .phero .wrap{position:relative;z-index:2}
  .phero h1{font-size:clamp(40px,5.6vw,74px);margin-top:16px}
  .phero p{font-size:19px;color:var(--muted-light);max-width:34em;margin-top:20px}
  .pgrid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}
  .pgrid.two{grid-template-columns:repeat(2,1fr)}
  .pcard{background:#fff;border:1px solid var(--line);border-radius:16px;padding:30px 26px}
  .sec--navy .pcard{background:rgba(246,241,230,.06);border-color:var(--line-light)}
  .pcard .pi{width:48px;height:48px;border-radius:12px;background:var(--cream-2);color:var(--blue);display:flex;align-items:center;justify-content:center;font-size:22px;font-weight:700;margin-bottom:18px}
  .sec--navy .pcard .pi{background:rgba(52,84,140,.2);color:var(--gold)}
  .pcard h3{font-family:"Instrument Serif",serif;font-size:22px;font-weight:500;margin-bottom:9px}
  .pcard p{font-size:15px;color:var(--muted);line-height:1.55;margin:0}
  .sec--navy .pcard p{color:var(--muted-light)}
  .blogcard{background:#fff;border:1px solid var(--line);border-radius:16px;overflow:hidden;display:flex;flex-direction:column;transition:transform .2s ease,box-shadow .2s ease}
  .blogcard:hover{transform:translateY(-4px);box-shadow:0 18px 40px -24px rgba(11,31,63,.4)}
  .blogcard .bimg{aspect-ratio:16/10;background:linear-gradient(150deg,var(--blue),var(--navy));position:relative}
  .blogcard .cat{position:absolute;top:14px;left:14px;background:var(--gold);color:var(--navy);font-size:11px;font-weight:700;letter-spacing:.05em;text-transform:uppercase;padding:5px 11px;border-radius:100px}
  .blogcard .bbody{padding:22px 22px 26px;flex:1;display:flex;flex-direction:column}
  .blogcard h3{font-family:"Instrument Serif",serif;font-size:20px;font-weight:500;margin-bottom:8px;line-height:1.2}
  .blogcard p{font-size:14.5px;color:var(--muted);margin:0 0 16px;flex:1}
  .blogcard .rd{font-size:14px;font-weight:600;color:var(--blue)}
  .pform{background:#fff;border:1px solid var(--line);border-radius:20px;padding:36px;display:grid;grid-template-columns:1fr 1fr;gap:16px}
  .pform .full{grid-column:1/-1}
  .pfield{display:flex;flex-direction:column;gap:6px}
  .pfield label{font-size:13px;font-weight:600;color:var(--navy)}
  .pfield input,.pfield select{font-family:inherit;font-size:15px;padding:13px 15px;border:1.5px solid var(--line);border-radius:10px;background:var(--cream);color:var(--ink)}
  .pfield input:focus,.pfield select:focus{outline:2px solid var(--blue);border-color:var(--blue)}
  @media (max-width:900px){.pgrid,.pgrid.two,.pform{grid-template-columns:1fr}.phero{padding:118px 0 56px}}
  /* --- página de simulação --- */
  .simgrid{display:grid;grid-template-columns:1.35fr .65fr;gap:22px;align-items:start}
  .simresult{background:var(--navy);color:var(--cream);border-radius:20px;padding:34px 30px;position:sticky;top:96px}
  .simr-label{font-size:14px;color:var(--muted-light);margin-top:18px}
  .simr-value{font-family:"Instrument Serif",serif;font-size:46px;font-weight:500;color:var(--gold);line-height:1;margin-top:4px}
  .simr-note{font-size:12.5px;color:var(--muted-light);margin-top:10px;line-height:1.5}
  .simr-list{list-style:none;margin-top:22px;padding-top:20px;border-top:1px solid var(--line-light);display:flex;flex-direction:column;gap:11px}
  .simr-list li{font-size:14.5px;color:var(--cream);padding-left:26px;position:relative}
  .simr-list li::before{content:"✓";position:absolute;left:0;color:var(--gold);font-weight:700}
  @media (max-width:900px){.simgrid{grid-template-columns:1fr}.simresult{position:static}}
  /* --- página de artigo --- */
  .artwrap{max-width:44rem;margin:0 auto}
  .artmeta{display:flex;align-items:center;gap:14px;font-size:13.5px;color:var(--muted);margin-bottom:34px}
  .artmeta .cat{background:var(--gold);color:var(--navy);font-size:11px;font-weight:700;letter-spacing:.05em;text-transform:uppercase;padding:5px 11px;border-radius:100px}
  .artbody p{font-size:17.5px;line-height:1.75;color:var(--ink);margin-bottom:22px}
  .artbody h2{font-family:"Instrument Serif",serif;font-size:28px;font-weight:500;line-height:1.15;margin:38px 0 14px}
  .artbody ul{margin:0 0 22px 22px}
  .artbody li{font-size:17px;line-height:1.7;color:var(--ink);margin-bottom:8px}
  .artcta{background:var(--navy);color:var(--cream);border-radius:20px;padding:36px 32px;margin-top:44px;text-align:center}
  .artcta h3{font-family:"Instrument Serif",serif;font-size:26px;font-weight:500;margin-bottom:8px}
  .artcta p{color:var(--muted-light);font-size:15.5px;margin-bottom:22px}
  .artback{display:inline-flex;align-items:center;gap:8px;font-size:14.5px;font-weight:600;color:var(--blue);margin-bottom:26px}
  .fsec{grid-column:1/-1;font-size:12px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--gold-deep);padding-top:14px;margin-top:4px;border-top:1px solid var(--line)}
  .fsec.first{border-top:none;padding-top:0;margin-top:0}
  .pfield input[readonly]{background:var(--cream-2);color:var(--muted)}
  .simhint{font-size:12.5px;color:var(--muted);line-height:1.5;margin-top:2px}
  .simhint.warn{color:var(--gold-deep);font-weight:600}
  .simneg{display:none;background:#6d2a1a;color:#f8ece5;border-radius:16px;padding:22px;margin-top:20px;font-size:14.5px;line-height:1.6}
  .simneg b{display:block;font-family:"Instrument Serif",serif;font-size:19px;font-weight:500;margin-bottom:7px}
  .simr-row{display:flex;justify-content:space-between;gap:12px;font-size:14px;color:var(--muted-light);padding:7px 0;border-bottom:1px dotted var(--line-light)}
  .simr-row b{color:var(--cream);font-weight:600;white-space:nowrap}
  .simrows{margin-top:20px}
  body.simneg-on .simr-main{display:none}
  /* --- wizard de simulação --- */
  .wiz{display:grid;grid-template-columns:.92fr 1.08fr;gap:64px;align-items:start}
  .wleft{position:sticky;top:104px}
  .wbadge{width:64px;height:64px;border-radius:22px;background:var(--cream-2);display:flex;align-items:center;justify-content:center;color:var(--blue);margin-bottom:26px}
  .wstepn{font-size:12.5px;font-weight:700;letter-spacing:.16em;text-transform:uppercase;color:var(--muted);margin-bottom:14px}
  .wleft h1{font-family:"Instrument Serif",serif;font-size:clamp(34px,4.2vw,52px);font-weight:500;line-height:1.06;letter-spacing:-.015em;color:var(--navy)}
  .wleft p{font-size:17px;color:var(--muted);margin-top:18px;max-width:26em;line-height:1.65}
  .wprog{display:flex;gap:8px;margin-top:30px}
  .wprog i{height:4px;flex:1;border-radius:4px;background:var(--line)}
  .wprog i.on{background:var(--gold)}
  .wpane{display:none}
  .wpane.on{display:block;animation:wfade .4s ease}
  @keyframes wfade{from{opacity:0;transform:translateY(14px)}to{opacity:1;transform:none}}
  .wq{font-family:"Instrument Serif",serif;font-size:26px;font-weight:500;color:var(--navy);margin-bottom:8px}
  .wqd{font-size:15.5px;color:var(--muted);margin-bottom:24px}
  .opts{display:flex;flex-direction:column;gap:12px}
  .opt{display:flex;align-items:center;justify-content:space-between;gap:16px;background:#fff;border:1.6px solid var(--line);border-radius:14px;padding:20px 22px;cursor:pointer;font-size:16px;font-weight:600;color:var(--navy);transition:border-color .15s ease,box-shadow .15s ease,transform .15s ease}
  .opt:hover{border-color:var(--blue);transform:translateY(-1px)}
  .opt .rad{flex:none;width:24px;height:24px;border-radius:50%;border:2px solid var(--line);position:relative;transition:border-color .15s ease}
  .opt.sel{border-color:var(--blue);box-shadow:0 10px 26px -16px rgba(52,84,140,.55)}
  .opt.sel .rad{border-color:var(--blue)}
  .opt.sel .rad::after{content:"";position:absolute;inset:4px;border-radius:50%;background:var(--blue)}
  .opt small{display:block;font-weight:500;color:var(--muted);font-size:13.5px;margin-top:3px}
  .fgrid{display:grid;grid-template-columns:1fr 1fr;gap:14px}
  .fgrid .full{grid-column:1/-1}
  .fcard{background:#fff;border:1.6px solid var(--line);border-radius:14px;padding:13px 18px 12px;transition:border-color .15s ease}
  .fcard:focus-within{border-color:var(--blue)}
  .fcard label{display:block;font-size:12.5px;font-weight:600;color:var(--muted)}
  .fcard input,.fcard select{width:100%;border:none;background:transparent;font-family:"Instrument Sans",sans-serif;font-size:17px;font-weight:600;color:var(--navy);padding:4px 0 0;outline:none}
  .fcard input::placeholder{color:rgba(90,107,132,.45);font-weight:500}
  .fcard input[readonly]{color:var(--muted)}
  .whint{font-size:13px;color:var(--muted);margin-top:8px;line-height:1.5}
  .whint.warn{color:var(--gold-deep);font-weight:600}
  .wnav{display:flex;gap:12px;margin-top:26px}
  .wnav .btn{flex:none}
  .btn.dis{opacity:.38;pointer-events:none}
  .west{background:var(--navy);color:var(--cream);border-radius:18px;padding:26px 26px 22px;margin-top:16px}
  .west .simr-label{font-size:13.5px;color:var(--muted-light)}
  .west .simr-value{font-family:"Instrument Serif",serif;font-size:40px;font-weight:500;color:var(--gold);line-height:1;margin-top:3px}
  .wiz .simneg{margin-top:16px}
  @media (max-width:900px){.wiz{grid-template-columns:1fr;gap:34px}.wleft{position:static}}
  /* --- mobile de verdade --- */
  .mburger{display:none;width:42px;height:42px;border-radius:12px;border:none;background:rgba(246,241,230,.14);cursor:pointer;flex-direction:column;align-items:center;justify-content:center;gap:4px;margin-left:4px}
  .mburger i{width:18px;height:2px;background:var(--cream);border-radius:2px;display:block}
  header.scrolled .mburger{background:rgba(11,31,63,.08)}
  header.scrolled .mburger i{background:var(--ink)}
  .mmenu{position:fixed;inset:0;background:var(--navy);z-index:120;display:flex;flex-direction:column;padding:22px;transform:translateY(-102%);transition:transform .32s cubic-bezier(.4,0,.1,1)}
  .mmenu.open{transform:none}
  .mmenu .top{display:flex;justify-content:space-between;align-items:center;margin-bottom:26px}
  .mmenu .logo{background-color:#f6f1e6}
  .mclose{width:42px;height:42px;border-radius:12px;border:none;background:rgba(246,241,230,.1);color:var(--cream);font-size:20px;cursor:pointer}
  .mmenu nav{display:flex;flex-direction:column}
  .mmenu nav a{font-family:"Instrument Serif",serif;font-size:27px;font-weight:500;color:var(--cream);padding:13px 0;border-bottom:1px solid var(--line-light)}
  .mmenu .mfoot{margin-top:auto;display:flex;flex-direction:column;gap:11px;padding-bottom:env(safe-area-inset-bottom,0px)}
  .mmenu .mfoot .btn{justify-content:center}
  @media (max-width:920px){
    .mburger{display:flex}
    .hero{padding:116px 0 60px}
    .phero{padding:116px 0 52px}
    .chip.c1{left:8px;bottom:14px}
    .chip.c2{right:8px;top:14px}
    .wleft{position:static}
  }
  @media (max-width:640px){
    h1{font-size:clamp(38px,11.5vw,54px)}
    .hero .lead{font-size:16.5px}
    .hero-cta{flex-direction:column;align-items:stretch}
    .hero-cta .btn{justify-content:center}
    .sec{padding:58px 0}
    .sec-head h2{font-size:clamp(27px,8vw,36px)}
    .cmp{padding:28px 24px}
    .cmp .big{font-size:clamp(44px,16vw,62px)}
    .quote blockquote{font-size:clamp(21px,6.4vw,28px)}
    .foot-top{grid-template-columns:1fr}
    .fgrid{grid-template-columns:1fr}
    .wleft h1{font-size:clamp(30px,9.5vw,40px)}
    .wq{font-size:22px}
    .wnav{flex-wrap:wrap}
    .wnav .btn{flex:1;justify-content:center;white-space:nowrap}
    .opt{padding:16px 18px;font-size:15px}
    .west{padding:20px 18px}
    .artbody p{font-size:16.5px}
    .adm-item{flex-direction:column;gap:8px}
    .adm-item .acts{padding-top:0}
    .about-stats{grid-template-columns:1fr 1fr;gap:10px}
    .about-stats .s{padding:16px}
    .about-stats .s b{font-size:30px}
    .steps{grid-template-columns:1fr}
    .cnav{margin-top:28px}
  }
  /* --- vídeo destaque --- */
  .vplayer{position:relative;aspect-ratio:16/9;max-width:980px;margin:0 auto;border-radius:24px;overflow:hidden;background:linear-gradient(150deg,#12294d,#0b1f3f);box-shadow:0 34px 70px -34px rgba(11,31,63,.55)}
  .vplayer video{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;background:#000}
  .vph{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;color:var(--muted-light);text-align:center;padding:24px}
  .vph b{font-family:"Instrument Serif",serif;font-size:22px;font-weight:500;color:var(--cream)}
  .vph span{font-size:14px;max-width:30em;line-height:1.55}
  .vph svg{opacity:.55;margin-bottom:4px}
  .vplay{position:absolute;inset:0;margin:auto;width:86px;height:86px;border-radius:50%;border:none;background:var(--gold);color:var(--navy);display:flex;align-items:center;justify-content:center;cursor:pointer;box-shadow:0 18px 40px -12px rgba(230,168,60,.7);transition:transform .18s ease}
  .vplay:hover{transform:scale(1.07)}
  .vplay svg{margin-left:5px}
  @media (max-width:640px){.vplay{width:62px;height:62px}.vplayer{border-radius:16px}}
  /* --- hero em tela cheia com vídeo --- */
  .hero-full{min-height:100svh;display:flex;align-items:center;position:relative;overflow:hidden;padding:120px 0 96px}
  .hero-full::after{display:none}
  .hero-video{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;z-index:0}
  .hero-fallback{position:absolute;inset:0;z-index:0}
  .hero-fallback img{width:100%;height:100%;object-fit:cover}
  .hero-shade{position:absolute;inset:0;z-index:1;background:linear-gradient(100deg,rgba(11,31,63,.95) 0%,rgba(11,31,63,.8) 42%,rgba(11,31,63,.45) 100%)}
  .hero-inner{position:relative;z-index:2;max-width:640px;margin-left:max(4vw,24px);margin-right:auto}
  .hero-inner .lead{max-width:27em}
  @media (max-width:920px){.hero-inner{margin-left:0}}
  .hero-chiprow{display:flex;gap:14px;margin-top:30px;flex-wrap:wrap}
  .hero-chiprow .chip{position:static}
  .hero-scroll{position:absolute;left:50%;bottom:24px;transform:translateX(-50%);z-index:2;width:26px;height:42px;border:2px solid rgba(246,241,230,.5);border-radius:14px}
  .hero-scroll span{position:absolute;left:50%;top:8px;width:4px;height:8px;margin-left:-2px;border-radius:2px;background:var(--gold);animation:sdrop 1.8s ease-in-out infinite}
  @keyframes sdrop{0%{transform:translateY(0);opacity:1}70%{transform:translateY(14px);opacity:0}100%{opacity:0}}
  @media (max-width:640px){.hero-full{padding:104px 0 78px}.hero-scroll{display:none}}
  /* Instrument Serif só tem peso 400: normaliza títulos pra evitar negrito sintético */
  h1,h2,h3,.serif,.wq,.step .num,.cmp .big,.cmp-meta .mk,.simr-value,.chip b,.quote blockquote,.artbody h2,.about-stats .s b,.vph b,.wleft h1,.brand,.mmenu nav a,.artcta h3,.foot .brand,.ap-head b,.adm-login .wq{font-weight:400}
  /* --- degradê do vídeo pra seção de baixo --- */
  .hero-fade{position:absolute;left:0;right:0;bottom:-1px;height:200px;z-index:1;pointer-events:none;background:linear-gradient(to bottom,rgba(246,241,230,0) 0%,rgba(246,241,230,.5) 55%,var(--cream) 100%)}
  .hero-scroll{border-color:rgba(11,31,63,.4)}
  .sec--blue .eyebrow{color:#fff}
  .sec--blue .eyebrow::before{background:var(--gold)}
  /* --- filtros de tema no blog --- */
  .blogfilters{display:flex;flex-wrap:wrap;gap:10px;margin-bottom:30px}
  .bchip{border:1.5px solid var(--line);background:#fff;border-radius:100px;padding:9px 18px;font-family:inherit;font-size:13.5px;font-weight:700;color:var(--muted);cursor:pointer;transition:all .15s ease}
  .bchip:hover{border-color:var(--navy);color:var(--navy)}
  .bchip.on{background:var(--navy);color:var(--cream);border-color:var(--navy)}
  /* --- modal de confirmação (form parceiros) --- */
  .pmodal{position:fixed;inset:0;z-index:420;display:flex;align-items:center;justify-content:center;padding:24px}
  .pmodal[hidden]{display:none}
  .pmodal-back{position:absolute;inset:0;background:rgba(11,31,63,.58);backdrop-filter:blur(3px)}
  .pmodal-card{position:relative;background:#fff;border:1px solid var(--line);border-radius:20px;padding:44px 38px 38px;max-width:440px;width:100%;text-align:center;box-shadow:0 30px 80px rgba(11,31,63,.38);animation:pmodalIn .3s cubic-bezier(.2,.9,.3,1.15)}
  @keyframes pmodalIn{from{opacity:0;transform:translateY(16px) scale(.96)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion:reduce){.pmodal-card{animation:none}}
  .pmodal-x{position:absolute;top:14px;right:14px;width:34px;height:34px;border:none;border-radius:50%;background:var(--cream);color:var(--navy);font-size:15px;line-height:1;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:background .18s ease}
  .pmodal-x:hover{background:var(--cream-2)}
  .pmodal-check{width:66px;height:66px;margin:0 auto 18px;border-radius:50%;background:rgba(230,168,60,.18);color:var(--gold-deep);display:flex;align-items:center;justify-content:center}
  .pmodal-card h3{font-size:30px;color:var(--navy);margin-bottom:10px}
  .pmodal-card p{font-size:15px;color:var(--muted);margin-bottom:26px}
  .pmodal-card .btn{width:100%;justify-content:center}
  /* --- validação do form de parceiros --- */
  .pfield input.err,.pfield select.err{border-color:#c25454;background:#fdf2ef}
  .pform-msg{grid-column:1/-1;font-size:13.5px;font-weight:600;color:#c25454;margin:-2px 0 0}
  /* --- seletor de sistema de amortização (Price/SAC) --- */
  .systog{display:flex;gap:5px;background:rgba(246,241,230,.08);border:1px solid var(--line-light);border-radius:100px;padding:5px;margin-bottom:2px}
  .sysopt{flex:1;border:none;border-radius:100px;padding:8px 10px;font-family:inherit;font-size:13px;font-weight:700;color:var(--muted-light);background:transparent;cursor:pointer;transition:background .15s ease,color .15s ease;display:flex;flex-direction:column;align-items:center;line-height:1.25}
  .sysopt small{font-size:10.5px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;opacity:.7}
  .sysopt.on{background:var(--cream);color:var(--navy)}
  .sysopt:not(.on):hover{color:var(--cream)}
  .sac-evo{display:flex;gap:8px;margin-top:12px}
  .sev{flex:1;background:rgba(246,241,230,.06);border:1px solid var(--line-light);border-radius:12px;padding:8px 6px;text-align:center}
  .sev span{display:block;font-size:10.5px;font-weight:700;color:var(--muted-light);text-transform:uppercase;letter-spacing:.05em}
  .sev b{display:block;font-size:14px;color:var(--cream);font-weight:600;margin-top:2px;white-space:nowrap;font-variant-numeric:tabular-nums}
  .sev.gd b{color:var(--gold)}
  @media (max-width:520px){.sev b{font-size:12.3px}.sev{padding:8px 4px}}
  .fcard.err{border-color:#c25454;background:#fdf2ef}
  /* --- rodapé legal --- */
  .legal-col p+p{margin-top:8px}
  .legal-links a{color:rgba(246,241,230,.72);text-decoration:none;border-bottom:1px solid rgba(246,241,230,.25);padding-bottom:1px;transition:color .15s ease,border-color .15s ease}
  .legal-links a:hover{color:var(--cream);border-color:var(--gold)}
  .legal-note{max-width:36em;font-size:12px;color:rgba(246,241,230,.42)}
  @media (max-width:720px){.legal{flex-direction:column}}
  /* --- páginas de políticas --- */
  .doc-pending{background:#fff;border:1.6px dashed var(--line);border-radius:18px;padding:36px 30px;text-align:center;margin-top:6px}
  .doc-pending b{display:block;font-family:"Instrument Serif",serif;font-size:22px;font-weight:500;color:var(--navy);margin-bottom:8px}
  .doc-pending p{color:var(--muted);font-size:15px;line-height:1.65;max-width:36em;margin:0 auto 20px}
  .doc-pending .btn{display:inline-flex;margin:4px 5px 0}
  .artbody ol{margin:0 0 22px 22px}
  .about-stats .s .apde{display:block;font-style:normal;font-size:11.5px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--muted-light);margin-bottom:7px}
  @media (max-width:640px){.about-stats .s .apde{font-size:10.5px;letter-spacing:.1em}}
  .mail-toast{position:fixed;bottom:26px;left:50%;transform:translateX(-50%) translateY(8px);background:var(--navy);color:var(--cream);border:1px solid rgba(246,241,230,.18);border-radius:100px;padding:12px 22px;font-size:14px;font-weight:600;z-index:500;opacity:0;pointer-events:none;transition:opacity .2s ease,transform .2s ease;white-space:nowrap;box-shadow:0 12px 30px rgba(8,23,51,.35)}
  .mail-toast.show{opacity:1;transform:translateX(-50%) translateY(0)}
  .mail-toast b{color:var(--gold)}
  @media (max-width:640px){.mail-toast{white-space:normal;text-align:center;max-width:calc(100vw - 40px);border-radius:16px}}
  /* --- seção Brasil inteiro --- */
  .br-grid{display:grid;grid-template-columns:1.05fr .95fr;gap:60px;align-items:center}
  .br-lead{color:var(--muted-light);font-size:17px;line-height:1.75;margin-top:18px;max-width:34em}
  .br-chips{display:flex;gap:12px;margin-top:32px;flex-wrap:wrap}
  .br-chip{background:rgba(246,241,230,.07);border:1px solid var(--line-light);border-radius:14px;padding:14px 18px;flex:1;min-width:150px}
  .br-chip b{display:block;color:var(--gold);font-size:16.5px;margin-bottom:2px}
  .br-chip span{font-size:12.5px;color:var(--muted-light)}
  .br-map{width:100%;max-width:470px;height:auto;display:block;margin:0 auto;filter:drop-shadow(0 20px 44px rgba(0,0,0,.35))}
  .br-ring{fill:none;stroke:var(--gold);stroke-width:1.6;opacity:0;transform-box:fill-box;transform-origin:center;animation:brping 3s ease-out infinite;animation-delay:var(--d,0s)}
  .br-hq .br-ring.r2{animation-delay:calc(var(--d,0s) + 1.5s)}
  @keyframes brping{0%{transform:scale(.55);opacity:.95}70%{transform:scale(2.5);opacity:0}100%{transform:scale(2.5);opacity:0}}
  .br-legend{display:flex;align-items:center;gap:8px;justify-content:center;margin-top:16px;font-size:13px;color:var(--muted-light)}
  .br-legend i{width:9px;height:9px;border-radius:50%;background:var(--gold);display:inline-block}
  .br-marquee{overflow:hidden;margin-top:56px;-webkit-mask-image:linear-gradient(90deg,transparent,#000 10%,#000 90%,transparent);mask-image:linear-gradient(90deg,transparent,#000 10%,#000 90%,transparent)}
  .br-track{display:flex;gap:38px;width:max-content;animation:brmove 60s linear infinite}
  .br-track span{font-family:"Instrument Serif",serif;font-style:italic;font-size:21px;color:rgba(246,241,230,.32);white-space:nowrap}
  @keyframes brmove{to{transform:translateX(-50%)}}
  @media (max-width:920px){.br-grid{grid-template-columns:1fr;gap:40px}.br-map{max-width:360px}}
  @media (max-width:640px){.br-lead{font-size:15.5px}.br-chip{min-width:130px;padding:12px 14px}.br-track span{font-size:18px}.br-marquee{margin-top:40px}}
  @media (prefers-reduced-motion:reduce){.br-track,.br-ring{animation:none}.br-ring{opacity:0}}
  /* --- players de vídeo (home + depoimento) --- */
  .vshell{position:relative;border-radius:20px;overflow:hidden;background:var(--navy);box-shadow:0 24px 60px rgba(8,23,51,.22);aspect-ratio:16/9;max-width:920px;margin:0 auto}
  .vshell video{width:100%;height:100%;object-fit:cover;display:block;background:#000}
  .vshell iframe{width:100%;height:100%;border:0;display:block;background:#000}
  .vempty{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;gap:8px;border:2px dashed rgba(246,241,230,.35);border-radius:20px;color:var(--cream);padding:28px}
  .vempty b{font-family:"Instrument Serif",serif;font-size:24px;font-weight:500}
  .vempty p{color:var(--muted-light);font-size:14px;max-width:34em;line-height:1.6;margin:0}
  .vempty .vico{width:56px;height:56px;border-radius:50%;background:var(--gold);color:var(--navy);display:flex;align-items:center;justify-content:center;margin-bottom:6px}
  .vsec[data-empty="1"]{display:none}
  .vdepo-wrap{display:grid;grid-template-columns:1.08fr .92fr;gap:56px;align-items:center;margin:0 0 48px}
  .vdepo-wrap .vshell{margin:0;max-width:none;position:relative;z-index:1}
  .vstage{position:relative}
  .vstage::before{content:"";position:absolute;top:18px;right:-18px;bottom:-18px;left:18px;border:2px solid var(--gold);border-radius:26px;opacity:.45;pointer-events:none}
  .vdepo-side h3{font-size:clamp(26px,3vw,38px);line-height:1.12;margin:14px 0 16px;font-weight:500}
  .vdepo-side p{color:var(--muted);font-size:16px;line-height:1.75;max-width:30em}
  .vchips{display:flex;gap:10px;margin-top:22px;flex-wrap:wrap}
  .vchip{display:inline-flex;align-items:center;gap:8px;border:1.5px solid var(--line);background:#fff;border-radius:100px;padding:9px 16px;font-size:13px;font-weight:700;color:var(--navy)}
  .vchip .pl{color:var(--gold);font-size:11px}
  @media (max-width:920px){.vdepo-wrap{grid-template-columns:1fr;gap:30px;margin-bottom:38px}.vstage::before{top:10px;right:-10px;bottom:-10px;left:10px;border-radius:18px}}
  @media (max-width:640px){.vshell{border-radius:14px}.vempty b{font-size:20px}.vempty p{font-size:13px}}
  /* ===== Polimento mobile ===== */
  img{max-width:100%}
  /* páginas internas (Como funciona, A Libra, Blog, Parceiros, Por quê) */
  @media (max-width:640px){
    .phero h1{font-size:clamp(29px,9vw,40px);margin-top:12px}
    .phero p{font-size:16px;margin-top:14px}
    .pcard{padding:24px 20px}
    .pcard h3{font-size:20px}
    .use{gap:14px;padding:18px 2px}
    .use .ui{width:40px;height:40px;font-size:19px;border-radius:10px}
    .use h3{font-size:20px}
    .cmp-meta{gap:24px;margin-top:26px;padding-top:20px}
    .cmp-meta .mk{font-size:28px}
    .blogcard .bbody{padding:18px 18px 22px}
    .blogfilters{gap:8px;margin-bottom:24px}
    .bchip{padding:8px 14px;font-size:13px}
  }
  @media (max-width:920px){
    html,body{overflow-x:hidden}
  }
  @media (max-width:640px){
    /* modais confortáveis no polegar */
    .pmodal{padding:14px}
    .pmodal-card{padding:34px 22px 26px}
    /* inputs com 16px: iPhone para de dar zoom sozinho ao focar */
    .pfield input,.pfield select{font-size:16px}
    /* cartões e CTAs respiram melhor */
    .doc-pending{padding:28px 18px}
    .doc-pending .btn{width:100%;margin:8px 0 0}
    .artcta{padding:30px 22px}
    .simr-note{font-size:11.8px}
  }
  @media (max-width:380px){
    .wrap{padding:0 16px}
    .west .simr-value{font-size:33px}
    .sysopt{padding:7px 6px;font-size:12.3px}
    .sysopt small{font-size:9.8px}
    .btn:not(.btn-sm){padding:14px 20px;font-size:14.5px}
    .pmodal-card h3{font-size:23px}
  }
  .fcard input[readonly]{color:var(--muted)}
  .whint{font-size:13px;color:var(--muted);margin-top:8px;line-height:1.5}
  .whint.warn{color:var(--gold-deep);font-weight:600}
  .wnav{display:flex;gap:12px;margin-top:26px}
  .wnav .btn{flex:none}
  .btn.dis{opacity:.38;pointer-events:none}
  .west{background:var(--navy);color:var(--cream);border-radius:18px;padding:26px 26px 22px;margin-top:16px}
  .west .simr-label{font-size:13.5px;color:var(--muted-light)}
  .west .simr-value{font-family:"Instrument Serif",serif;font-size:40px;font-weight:500;color:var(--gold);line-height:1;margin-top:3px}
  .wiz .simneg{margin-top:16px}
  @media (max-width:900px){.wiz{grid-template-columns:1fr;gap:34px}.wleft{position:static}}
  /* --- mobile de verdade --- */
  .mburger{display:none;width:42px;height:42px;border-radius:12px;border:none;background:rgba(246,241,230,.14);cursor:pointer;flex-direction:column;align-items:center;justify-content:center;gap:4px;margin-left:4px}
  .mburger i{width:18px;height:2px;background:var(--cream);border-radius:2px;display:block}
  header.scrolled .mburger{background:rgba(11,31,63,.08)}
  header.scrolled .mburger i{background:var(--ink)}
  .mmenu{position:fixed;inset:0;background:var(--navy);z-index:120;display:flex;flex-direction:column;padding:22px;transform:translateY(-102%);transition:transform .32s cubic-bezier(.4,0,.1,1)}
  .mmenu.open{transform:none}
  .mmenu .top{display:flex;justify-content:space-between;align-items:center;margin-bottom:26px}
  .mmenu .logo{background-color:#f6f1e6}
  .mclose{width:42px;height:42px;border-radius:12px;border:none;background:rgba(246,241,230,.1);color:var(--cream);font-size:20px;cursor:pointer}
  .mmenu nav{display:flex;flex-direction:column}
  .mmenu nav a{font-family:"Instrument Serif",serif;font-size:27px;font-weight:500;color:var(--cream);padding:13px 0;border-bottom:1px solid var(--line-light)}
  .mmenu .mfoot{margin-top:auto;display:flex;flex-direction:column;gap:11px;padding-bottom:env(safe-area-inset-bottom,0px)}
  .mmenu .mfoot .btn{justify-content:center}
  @media (max-width:920px){
    .mburger{display:flex}
    .hero{padding:116px 0 60px}
    .phero{padding:116px 0 52px}
    .chip.c1{left:8px;bottom:14px}
    .chip.c2{right:8px;top:14px}
    .wleft{position:static}
  }
  @media (max-width:640px){
    h1{font-size:clamp(38px,11.5vw,54px)}
    .hero .lead{font-size:16.5px}
    .hero-cta{flex-direction:column;align-items:stretch}
    .hero-cta .btn{justify-content:center}
    .sec{padding:58px 0}
    .sec-head h2{font-size:clamp(27px,8vw,36px)}
    .cmp{padding:28px 24px}
    .cmp .big{font-size:clamp(44px,16vw,62px)}
    .quote blockquote{font-size:clamp(21px,6.4vw,28px)}
    .foot-top{grid-template-columns:1fr}
    .fgrid{grid-template-columns:1fr}
    .wleft h1{font-size:clamp(30px,9.5vw,40px)}
    .wq{font-size:22px}
    .wnav{flex-wrap:wrap}
    .wnav .btn{flex:1;justify-content:center;white-space:nowrap}
    .opt{padding:16px 18px;font-size:15px}
    .west{padding:20px 18px}
    .artbody p{font-size:16.5px}
    .adm-item{flex-direction:column;gap:8px}
    .adm-item .acts{padding-top:0}
    .about-stats{grid-template-columns:1fr 1fr;gap:10px}
    .about-stats .s{padding:16px}
    .about-stats .s b{font-size:30px}
    .steps{grid-template-columns:1fr}
    .cnav{margin-top:28px}
  }
  /* --- vídeo destaque --- */
  .vplayer{position:relative;aspect-ratio:16/9;max-width:980px;margin:0 auto;border-radius:24px;overflow:hidden;background:linear-gradient(150deg,#12294d,#0b1f3f);box-shadow:0 34px 70px -34px rgba(11,31,63,.55)}
  .vplayer video{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;background:#000}
  .vph{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;color:var(--muted-light);text-align:center;padding:24px}
  .vph b{font-family:"Instrument Serif",serif;font-size:22px;font-weight:500;color:var(--cream)}
  .vph span{font-size:14px;max-width:30em;line-height:1.55}
  .vph svg{opacity:.55;margin-bottom:4px}
  .vplay{position:absolute;inset:0;margin:auto;width:86px;height:86px;border-radius:50%;border:none;background:var(--gold);color:var(--navy);display:flex;align-items:center;justify-content:center;cursor:pointer;box-shadow:0 18px 40px -12px rgba(230,168,60,.7);transition:transform .18s ease}
  .vplay:hover{transform:scale(1.07)}
  .vplay svg{margin-left:5px}
  @media (max-width:640px){.vplay{width:62px;height:62px}.vplayer{border-radius:16px}}
  /* --- hero em tela cheia com vídeo --- */
  .hero-full{min-height:100svh;display:flex;align-items:center;position:relative;overflow:hidden;padding:120px 0 96px}
  .hero-full::after{display:none}
  .hero-video{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;z-index:0}
  .hero-fallback{position:absolute;inset:0;z-index:0}
  .hero-fallback img{width:100%;height:100%;object-fit:cover}
  .hero-shade{position:absolute;inset:0;z-index:1;background:linear-gradient(100deg,rgba(11,31,63,.95) 0%,rgba(11,31,63,.8) 42%,rgba(11,31,63,.45) 100%)}
  .hero-inner{position:relative;z-index:2;max-width:640px;margin-left:max(4vw,24px);margin-right:auto}
  .hero-inner .lead{max-width:27em}
  @media (max-width:920px){.hero-inner{margin-left:0}}
  .hero-chiprow{display:flex;gap:14px;margin-top:30px;flex-wrap:wrap}
  .hero-chiprow .chip{position:static}
  .hero-scroll{position:absolute;left:50%;bottom:24px;transform:translateX(-50%);z-index:2;width:26px;height:42px;border:2px solid rgba(246,241,230,.5);border-radius:14px}
  .hero-scroll span{position:absolute;left:50%;top:8px;width:4px;height:8px;margin-left:-2px;border-radius:2px;background:var(--gold);animation:sdrop 1.8s ease-in-out infinite}
  @keyframes sdrop{0%{transform:translateY(0);opacity:1}70%{transform:translateY(14px);opacity:0}100%{opacity:0}}
  @media (max-width:640px){.hero-full{padding:104px 0 78px}.hero-scroll{display:none}}
  /* Instrument Serif só tem peso 400: normaliza títulos pra evitar negrito sintético */
  h1,h2,h3,.serif,.wq,.step .num,.cmp .big,.cmp-meta .mk,.simr-value,.chip b,.quote blockquote,.artbody h2,.about-stats .s b,.vph b,.wleft h1,.brand,.mmenu nav a,.artcta h3,.foot .brand,.ap-head b,.adm-login .wq{font-weight:400}
  /* --- degradê do vídeo pra seção de baixo --- */
  .hero-fade{position:absolute;left:0;right:0;bottom:-1px;height:200px;z-index:1;pointer-events:none;background:linear-gradient(to bottom,rgba(246,241,230,0) 0%,rgba(246,241,230,.5) 55%,var(--cream) 100%)}
  .hero-scroll{border-color:rgba(11,31,63,.4)}
  .sec--blue .eyebrow{color:#fff}
  .sec--blue .eyebrow::before{background:var(--gold)}
  /* --- filtros de tema no blog --- */
  .blogfilters{display:flex;flex-wrap:wrap;gap:10px;margin-bottom:30px}
  .bchip{border:1.5px solid var(--line);background:#fff;border-radius:100px;padding:9px 18px;font-family:inherit;font-size:13.5px;font-weight:700;color:var(--muted);cursor:pointer;transition:all .15s ease}
  .bchip:hover{border-color:var(--navy);color:var(--navy)}
  .bchip.on{background:var(--navy);color:var(--cream);border-color:var(--navy)}
  /* --- modal de confirmação (form parceiros) --- */
  .pmodal{position:fixed;inset:0;z-index:420;display:flex;align-items:center;justify-content:center;padding:24px}
  .pmodal[hidden]{display:none}
  .pmodal-back{position:absolute;inset:0;background:rgba(11,31,63,.58);backdrop-filter:blur(3px)}
  .pmodal-card{position:relative;background:#fff;border:1px solid var(--line);border-radius:20px;padding:44px 38px 38px;max-width:440px;width:100%;text-align:center;box-shadow:0 30px 80px rgba(11,31,63,.38);animation:pmodalIn .3s cubic-bezier(.2,.9,.3,1.15)}
  @keyframes pmodalIn{from{opacity:0;transform:translateY(16px) scale(.96)}to{opacity:1;transform:none}}
  @media (prefers-reduced-motion:reduce){.pmodal-card{animation:none}}
  .pmodal-x{position:absolute;top:14px;right:14px;width:34px;height:34px;border:none;border-radius:50%;background:var(--cream);color:var(--navy);font-size:15px;line-height:1;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:background .18s ease}
  .pmodal-x:hover{background:var(--cream-2)}
  .pmodal-check{width:66px;height:66px;margin:0 auto 18px;border-radius:50%;background:rgba(230,168,60,.18);color:var(--gold-deep);display:flex;align-items:center;justify-content:center}
  .pmodal-card h3{font-size:30px;color:var(--navy);margin-bottom:10px}
  .pmodal-card p{font-size:15px;color:var(--muted);margin-bottom:26px}
  .pmodal-card .btn{width:100%;justify-content:center}
  /* --- validação do form de parceiros --- */
  .pfield input.err,.pfield select.err{border-color:#c25454;background:#fdf2ef}
  .pform-msg{grid-column:1/-1;font-size:13.5px;font-weight:600;color:#c25454;margin:-2px 0 0}
  /* --- seletor de sistema de amortização (Price/SAC) --- */
  .systog{display:flex;gap:5px;background:rgba(246,241,230,.08);border:1px solid var(--line-light);border-radius:100px;padding:5px;margin-bottom:2px}
  .sysopt{flex:1;border:none;border-radius:100px;padding:8px 10px;font-family:inherit;font-size:13px;font-weight:700;color:var(--muted-light);background:transparent;cursor:pointer;transition:background .15s ease,color .15s ease;display:flex;flex-direction:column;align-items:center;line-height:1.25}
  .sysopt small{font-size:10.5px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;opacity:.7}
  .sysopt.on{background:var(--cream);color:var(--navy)}
  .sysopt:not(.on):hover{color:var(--cream)}
  .sac-evo{display:flex;gap:8px;margin-top:12px}
  .sev{flex:1;background:rgba(246,241,230,.06);border:1px solid var(--line-light);border-radius:12px;padding:8px 6px;text-align:center}
  .sev span{display:block;font-size:10.5px;font-weight:700;color:var(--muted-light);text-transform:uppercase;letter-spacing:.05em}
  .sev b{display:block;font-size:14px;color:var(--cream);font-weight:600;margin-top:2px;white-space:nowrap;font-variant-numeric:tabular-nums}
  .sev.gd b{color:var(--gold)}
  @media (max-width:520px){.sev b{font-size:12.3px}.sev{padding:8px 4px}}
  .fcard.err{border-color:#c25454;background:#fdf2ef}
  /* --- rodapé legal --- */
  .legal-col p+p{margin-top:8px}
  .legal-links a{color:rgba(246,241,230,.72);text-decoration:none;border-bottom:1px solid rgba(246,241,230,.25);padding-bottom:1px;transition:color .15s ease,border-color .15s ease}
  .legal-links a:hover{color:var(--cream);border-color:var(--gold)}
  .legal-note{max-width:36em;font-size:12px;color:rgba(246,241,230,.42)}
  @media (max-width:720px){.legal{flex-direction:column}}
  /* --- páginas de políticas --- */
  .doc-pending{background:#fff;border:1.6px dashed var(--line);border-radius:18px;padding:36px 30px;text-align:center;margin-top:6px}
  .doc-pending b{display:block;font-family:"Instrument Serif",serif;font-size:22px;font-weight:500;color:var(--navy);margin-bottom:8px}
  .doc-pending p{color:var(--muted);font-size:15px;line-height:1.65;max-width:36em;margin:0 auto 20px}
  .doc-pending .btn{display:inline-flex;margin:4px 5px 0}
  .artbody ol{margin:0 0 22px 22px}
  .about-stats .s .apde{display:block;font-style:normal;font-size:11.5px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--muted-light);margin-bottom:7px}
  @media (max-width:640px){.about-stats .s .apde{font-size:10.5px;letter-spacing:.1em}}
  .mail-toast{position:fixed;bottom:26px;left:50%;transform:translateX(-50%) translateY(8px);background:var(--navy);color:var(--cream);border:1px solid rgba(246,241,230,.18);border-radius:100px;padding:12px 22px;font-size:14px;font-weight:600;z-index:500;opacity:0;pointer-events:none;transition:opacity .2s ease,transform .2s ease;white-space:nowrap;box-shadow:0 12px 30px rgba(8,23,51,.35)}
  .mail-toast.show{opacity:1;transform:translateX(-50%) translateY(0)}
  .mail-toast b{color:var(--gold)}
  @media (max-width:640px){.mail-toast{white-space:normal;text-align:center;max-width:calc(100vw - 40px);border-radius:16px}}
  /* --- seção Brasil inteiro --- */
  .br-grid{display:grid;grid-template-columns:1.05fr .95fr;gap:60px;align-items:center}
  .br-lead{color:var(--muted-light);font-size:17px;line-height:1.75;margin-top:18px;max-width:34em}
  .br-chips{display:flex;gap:12px;margin-top:32px;flex-wrap:wrap}
  .br-chip{background:rgba(246,241,230,.07);border:1px solid var(--line-light);border-radius:14px;padding:14px 18px;flex:1;min-width:150px}
  .br-chip b{display:block;color:var(--gold);font-size:16.5px;margin-bottom:2px}
  .br-chip span{font-size:12.5px;color:var(--muted-light)}
  .br-map{width:100%;max-width:470px;height:auto;display:block;margin:0 auto;filter:drop-shadow(0 20px 44px rgba(0,0,0,.35))}
  .br-ring{fill:none;stroke:var(--gold);stroke-width:1.6;opacity:0;transform-box:fill-box;transform-origin:center;animation:brping 3s ease-out infinite;animation-delay:var(--d,0s)}
  .br-hq .br-ring.r2{animation-delay:calc(var(--d,0s) + 1.5s)}
  @keyframes brping{0%{transform:scale(.55);opacity:.95}70%{transform:scale(2.5);opacity:0}100%{transform:scale(2.5);opacity:0}}
  .br-legend{display:flex;align-items:center;gap:8px;justify-content:center;margin-top:16px;font-size:13px;color:var(--muted-light)}
  .br-legend i{width:9px;height:9px;border-radius:50%;background:var(--gold);display:inline-block}
  .br-marquee{overflow:hidden;margin-top:56px;-webkit-mask-image:linear-gradient(90deg,transparent,#000 10%,#000 90%,transparent);mask-image:linear-gradient(90deg,transparent,#000 10%,#000 90%,transparent)}
  .br-track{display:flex;gap:38px;width:max-content;animation:brmove 60s linear infinite}
  .br-track span{font-family:"Instrument Serif",serif;font-style:italic;font-size:21px;color:rgba(246,241,230,.32);white-space:nowrap}
  @keyframes brmove{to{transform:translateX(-50%)}}
  @media (max-width:920px){.br-grid{grid-template-columns:1fr;gap:40px}.br-map{max-width:360px}}
  @media (max-width:640px){.br-lead{font-size:15.5px}.br-chip{min-width:130px;padding:12px 14px}.br-track span{font-size:18px}.br-marquee{margin-top:40px}}
  @media (prefers-reduced-motion:reduce){.br-track,.br-ring{animation:none}.br-ring{opacity:0}}
  /* --- players de vídeo (home + depoimento) --- */
  .vshell{position:relative;border-radius:20px;overflow:hidden;background:var(--navy);box-shadow:0 24px 60px rgba(8,23,51,.22);aspect-ratio:16/9;max-width:920px;margin:0 auto}
  .vshell video{width:100%;height:100%;object-fit:cover;display:block;background:#000}
  .vshell iframe{width:100%;height:100%;border:0;display:block;background:#000}
  .vempty{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;gap:8px;border:2px dashed rgba(246,241,230,.35);border-radius:20px;color:var(--cream);padding:28px}
  .vempty b{font-family:"Instrument Serif",serif;font-size:24px;font-weight:500}
  .vempty p{color:var(--muted-light);font-size:14px;max-width:34em;line-height:1.6;margin:0}
  .vempty .vico{width:56px;height:56px;border-radius:50%;background:var(--gold);color:var(--navy);display:flex;align-items:center;justify-content:center;margin-bottom:6px}
  .vsec[data-empty="1"]{display:none}
  .vdepo-wrap{display:grid;grid-template-columns:1.08fr .92fr;gap:56px;align-items:center;margin:0 0 48px}
  .vdepo-wrap .vshell{margin:0;max-width:none;position:relative;z-index:1}
  .vstage{position:relative}
  .vstage::before{content:"";position:absolute;top:18px;right:-18px;bottom:-18px;left:18px;border:2px solid var(--gold);border-radius:26px;opacity:.45;pointer-events:none}
  .vdepo-side h3{font-size:clamp(26px,3vw,38px);line-height:1.12;margin:14px 0 16px;font-weight:500}
  .vdepo-side p{color:var(--muted);font-size:16px;line-height:1.75;max-width:30em}
  .vchips{display:flex;gap:10px;margin-top:22px;flex-wrap:wrap}
  .vchip{display:inline-flex;align-items:center;gap:8px;border:1.5px solid var(--line);background:#fff;border-radius:100px;padding:9px 16px;font-size:13px;font-weight:700;color:var(--navy)}
  .vchip .pl{color:var(--gold);font-size:11px}
  @media (max-width:920px){.vdepo-wrap{grid-template-columns:1fr;gap:30px;margin-bottom:38px}.vstage::before{top:10px;right:-10px;bottom:-10px;left:10px;border-radius:18px}}
  @media (max-width:640px){.vshell{border-radius:14px}.vempty b{font-size:20px}.vempty p{font-size:13px}}
  /* ===== Polimento mobile ===== */
  img{max-width:100%}
  /* páginas internas (Como funciona, A Libra, Blog, Parceiros, Por quê) */
  @media (max-width:640px){
    .phero h1{font-size:clamp(29px,9vw,40px);margin-top:12px}
    .phero p{font-size:16px;margin-top:14px}
    .pcard{padding:24px 20px}
    .pcard h3{font-size:20px}
    .use{gap:14px;padding:18px 2px}
    .use .ui{width:40px;height:40px;font-size:19px;border-radius:10px}
    .use h3{font-size:20px}
    .cmp-meta{gap:24px;margin-top:26px;padding-top:20px}
    .cmp-meta .mk{font-size:28px}
    .blogcard .bbody{padding:18px 18px 22px}
    .blogfilters{gap:8px;margin-bottom:24px}
    .bchip{padding:8px 14px;font-size:13px}
  }
  @media (max-width:920px){
    html,body{overflow-x:hidden}
  }
  @media (max-width:640px){
    /* modais confortáveis no polegar */
    .pmodal{padding:14px}
    .pmodal-card{padding:34px 22px 26px}
    /* inputs com 16px: iPhone para de dar zoom sozinho ao focar */
    .pfield input,.pfield select{font-size:16px}
    /* cartões e CTAs respiram melhor */
    .doc-pending{padding:28px 18px}
    .doc-pending .btn{width:100%;margin:8px 0 0}
    .artcta{padding:30px 22px}
    .simr-note{font-size:11.8px}
  }
  @media (max-width:380px){
    .wrap{padding:0 16px}
    .west .simr-value{font-size:33px}
    .sysopt{padding:7px 6px;font-size:12.3px}
    .sysopt small{font-size:9.8px}
    .btn:not(.btn-sm){padding:14px 20px;font-size:14.5px}
    .pmodal-card h3{font-size:23px}
  }